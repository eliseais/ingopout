## ingopout\n\n### Обзор\nОдностраничное приложение на React + TypeScript с использованием Tailwind CSS v4 и набора UI‑компонентов (папка `components/ui`). Точка входа — `main.tsx`, корневой компонент — `App.tsx`, основной экран — `imports/Landing.tsx`.\n\n### Стек\n- React (ReactDOM `createRoot`)\n- TypeScript\n- Tailwind CSS v4 (`index.css`, `styles/globals.css`)\n- UI‑компоненты (`components/ui/*`, shadcn/radix‑стиль)\n\n### Структура\n- `App.tsx` — корневой компонент, подключает `imports/Landing.tsx`\n- `main.tsx` — монтирование приложения и подключение `index.css`\n- `imports/` — страницы/секции, в т.ч. `Landing.tsx`\n- `components/` — переиспользуемые UI‑компоненты\n- `assets/` — изображения и статические ресурсы\n- `styles/` — глобальные стили (`globals.css`)\n\n### Требования\n- Node.js 18+ (рекомендуется LTS)\n- npm / pnpm / yarn — на ваш выбор\n\n### Запуск (локально)\nТак как проект выглядит как типичная Vite‑сборка React+TS, используйте скрипты из `package.json` вашего окружения:\n\n```bash\n# с npm\nnpm install\nnpm run dev\n\n# или с pnpm\npnpm install\npnpm dev\n\n# или с yarn\nyarn\nyarn dev\n```\n\nЕсли у вас нет сборщика/скриптов, а в корне присутствует `index.html`, можно открыть его через статический сервер:\n\n```bash\n# пример через vite preview (если установлен)\nnpx vite preview --open\n\n# либо любой статический сервер, например:\nnpx serve -s .\n```\n\n### Билд\n```bash\n# npm\nnpm run build\n\n# pnpm\ndpnm build\n\n# yarn\nyarn build\n```\nАртефакты сборки обычно попадают в `dist/` (по умолчанию у Vite).\n\n### Атрибуции/лицензии\nСписок источников и лицензий для ассетов — в `Attributions.md`.\n\n### Полезные ссылки\n- Tailwind CSS v4 — `https://tailwindcss.com`\n- React — `https://react.dev`\n\n### Разработка\n- Рекомендуется включённый форматтер и линтер (ESLint/Prettier), соответствие общему стилю кода.\n- Компоненты в `components/ui/*` стараемся переиспользовать, кастомные стили — в `styles/globals.css` и с помощью utility‑классов Tailwind.\n*** End Patch``` }```

