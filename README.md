# ИНГОПУТЬ (ingopout)

Одностраничное приложение на React + TypeScript с использованием Tailwind CSS v4. Лендинг для мобильного приложения путешествий.

## 🚀 Быстрый старт

### Требования
- **Node.js** 18+ (рекомендуется LTS версия)
- **npm**, **pnpm** или **yarn** — менеджер пакетов

### Установка и запуск

1. **Перейдите в директорию проекта:**
   ```bash
   cd C:\mmm\ingopout-vite
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```
   или
   ```bash
   pnpm install
   ```
   или
   ```bash
   yarn install
   ```

3. **Запустите dev-сервер:**
   ```bash
   npm run dev
   ```
   или
   ```bash
   pnpm dev
   ```
   или
   ```bash
   yarn dev
   ```

4. **Откройте в браузере:**
   - Dev-сервер запустится на `http://localhost:5173/`
   - Страница автоматически обновится при изменении файлов (HMR)

## 📦 Сборка для production

### Создание production-сборки:
```bash
npm run build
```
или
```bash
pnpm build
```
или
```bash
yarn build
```

Собранные файлы будут в папке `dist/`.

### Предпросмотр production-сборки:
```bash
npm run preview
```
или
```bash
pnpm preview
```
или
```bash
yarn preview
```

## 🛠 Технологический стек

- **React** 19.2.0 — UI библиотека
- **TypeScript** 5.9.3 — типизация
- **Vite** 7.2.4 — сборщик и dev-сервер
- **Tailwind CSS v4** — utility-first CSS фреймворк
- **UI компоненты** — набор переиспользуемых компонентов в `components/ui/*`

## 📁 Структура проекта

```
ingopout-vite/
├── src/
│   ├── App.tsx              # Корневой компонент приложения
│   ├── main.tsx             # Точка входа (монтирование React)
│   ├── index.css            # Tailwind CSS utility-классы
│   ├── imports/
│   │   └── Landing.tsx      # Главная страница (лендинг)
│   ├── components/
│   │   ├── ui/              # UI компоненты (shadcn/radix стиль)
│   │   └── ResponsiveStage.tsx  # Компонент адаптивности
│   ├── assets/              # Изображения и статические ресурсы
│   └── styles/
│       └── globals.css      # Глобальные стили и CSS переменные
├── public/                  # Статические файлы
├── index.html               # HTML шаблон
├── package.json             # Зависимости и скрипты
├── tsconfig.json            # Конфигурация TypeScript
├── vite.config.ts           # Конфигурация Vite
└── README.md                # Этот файл
```

## 🎨 Особенности

- **Адаптивный дизайн** — автоматическое масштабирование под любой размер экрана
- **Плавная прокрутка** — smooth scroll для навигации по секциям
- **Оптимизированные изображения** — все ассеты импортируются как модули
- **TypeScript** — полная типизация для надежности кода

## 🔧 Доступные команды

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск dev-сервера с hot-reload |
| `npm run build` | Сборка production версии |
| `npm run preview` | Предпросмотр production сборки |
| `npm run lint` | Проверка кода линтером |

## ⚠️ Частые проблемы и решения

### Пустой экран после запуска
- Убедитесь, что в `index.html` есть элемент `<div id="root"></div>`
- Проверьте консоль браузера на наличие ошибок

### Стили не применяются
- Убедитесь, что в `src/main.tsx` импортированы оба файла стилей:
  ```ts
  import './index.css';
  import './styles/globals.css';
  ```

### Ошибки импорта изображений
- Все изображения должны находиться в `src/assets/`
- Импортируйте их как модули: `import img from '../assets/image.png'`

### Проблемы с путями
- Используйте относительные пути от `src/`
- Для статических файлов используйте папку `public/` и обращайтесь через `/filename.ext`

## 📝 Переменные окружения

Проект не требует специальных переменных окружения. Все настройки находятся в конфигурационных файлах.

## 📄 Лицензии и атрибуции

Информация об источниках и лицензиях для используемых ассетов находится в файле `Attributions.md`.

## 🔗 Полезные ссылки

- [Vite Documentation](https://vitejs.dev/)
- [React Documentation](https://react.dev/)
- [Tailwind CSS v4](https://tailwindcss.com/)
- [TypeScript Documentation](https://www.typescriptlang.org/)

## 👨‍💻 Разработка

### Рекомендации по стилю кода:
- Используйте компоненты из `components/ui/*` для переиспользования
- Кастомные стили добавляйте через utility-классы Tailwind
- Глобальные стили — в `styles/globals.css`
- Следуйте TypeScript best practices

### Структура компонентов:
- Страницы/секции — в `src/imports/`
- Переиспользуемые компоненты — в `src/components/`
- UI компоненты (кнопки, формы и т.д.) — в `src/components/ui/`

---

**Приятной разработки! 🚀**
